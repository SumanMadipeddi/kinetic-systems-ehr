import type { Provider } from "@/types/provider";

export const PROVIDERS: Provider[] = [
  {
    id: "prov-suman-ma",
    displayName: "Ma, suman",
    color: "#7b5ea7",
  },
];

export const DEFAULT_PROVIDER_ID = PROVIDERS[0].id;
