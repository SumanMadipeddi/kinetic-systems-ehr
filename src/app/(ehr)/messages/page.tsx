import { ShallowPage } from "@/components/layout/shallow-page";

export default function MessagesPage() {
  return (
    <ShallowPage
      title="Messages"
      description="Messages is a shallow navigation destination. Messaging and inbox workflows are outside the assessment scope."
    />
  );
}
